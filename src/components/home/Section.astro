---
const { title, subtitle } = Astro.props;
---
<section class="section">
  <div class="container">
    {title && <h2>{title}</h2>}
    {subtitle && <p class="mt-2 text-gray-600">{subtitle}</p>}
    <div class="mt-6">
      <slot />
    </div>
  </div>
</section>
